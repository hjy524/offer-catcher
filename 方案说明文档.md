# Offer捕手 - AI求职智能匹配平台 · 方案说明文档

## 一、项目概述

### 1.1 产品定位
Offer捕手是面向在校大学生、应届生、实习求职群体的一站式AI求职成长平台，覆盖职业迷茫疏导、简历智能优化、岗位精准匹配、个人求职沉淀、同龄人社区互助全场景。

### 1.2 核心价值
- **解决求职迷茫**：AI智能引导梳理职业方向，配套职业测评
- **提升求职效率**：多维度精准匹配适配岗位，一键诊断简历
- **优化简历质量**：AI诊断+针对性优化建议，对标JD查漏补缺
- **沉淀求职成长**：私密日志+公开社区双轨记录求职全过程

### 1.3 目标用户
- 在校大学生（实习求职）
- 应届毕业生（校招求职）
- 职场新人（社招求职）

---

## 二、技术架构

### 2.1 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | Vue | 3.4.x | 渐进式JavaScript框架 |
| 构建工具 | Vite | 5.4.x | 下一代前端构建工具 |
| UI组件 | Element Plus | 2.14.x | Vue3组件库 |
| 路由管理 | Vue Router | 4.3.x | History模式SPA路由 |
| 数据库 | Supabase | 免费版 | 云端PostgreSQL + localStorage双写 |
| AI服务 | 硅基流动API | - | DeepSeek-V4-Flash大模型 |
| 部署平台 | Vercel / Cloudflare Pages | 免费版 | 双平台可切换部署 |
| 包管理 | npm | - | 依赖管理 |

### 2.2 架构设计

```
┌────────────────────────────────────────────────────────────────────────┐
│                        前端展示层（Vue3 + Element Plus）                 │
│  首页 | 简历 | 岗位匹配 | JD诊断 | AI咨询 | 日志 | 社区 | 个人中心     │
├────────────────────────────────────────────────────────────────────────┤
│                        业务逻辑层                                      │
│  用户状态管理 / AI服务封装 / 数据读写层 / 工具函数                     │
├────────────────────────────────────────────────────────────────────────┤
│                        数据访问层（双写策略）                           │
│  ┌─────────────────────┐  ┌──────────────────────────────────────┐   │
│  │  localStorage (同步) │  │  Supabase PostgreSQL (异步)         │   │
│  │  即时读写 秒级响应   │  │  云端持久化 跨设备同步               │   │
│  └─────────────────────┘  └──────────────────────────────────────┘   │
├────────────────────────────────────────────────────────────────────────┤
│                        后端服务层                                      │
│  Supabase Auth / PostgreSQL RLS / 硅基流动AI API                      │
└────────────────────────────────────────────────────────────────────────┘
```

### 2.3 数据存储方案

采用 **localStorage + Supabase 双写策略**：

**写入流程：**
```
用户操作 → localStorage（同步写入，即时生效） → Supabase（异步推送到云端）
```

**读取流程：**
```
首次登录 → 从 Supabase 拉取数据到 localStorage
后续读写 → localStorage 同步响应（毫秒级）
```

**优势：**
- 兼顾速度（localStorage 秒级响应）和持久化（Supabase 云端存储）
- 离线可用（localStorage 缓存）
- 跨设备数据同步（同一账号登录自动拉取云端数据）

---

## 三、核心功能模块

### 3.1 页面路由总览

| 路径 | 页面名称 | 核心功能 |
|------|----------|----------|
| `/` | 首页 | 平台入口、快捷入口、核心功能介绍 |
| `/resume` | 简历智能处理 | PDF/TXT上传解析、在线制作、多版本管理 |
| `/job-match` | 岗位智能匹配 | 城市/公司/岗位类型筛选、薪资范围、收藏 |
| `/resume-check` | JD简历诊断优化 | AI逐项对比、匹配度评分、优化建议 |
| `/ai-consult` | AI职业规划咨询 | 流式对话、职业标签看板、测评外链 |
| `/journal` | 心路日志 | Flomo式快速记录、标签管理、日历看板、AI洞察 |
| `/community` | 求职笔记社区 | 分类浏览、图文发布、点赞评论互动 |
| `/article` | 求职干货 | 面试技巧、简历攻略、职业规划文章 |
| `/user` | 个人中心 | 简历/收藏/日志/笔记管理、诊断/AI对话历史、设置 |
| `/note/:id` | 笔记详情 | 个人模式（含侧边栏筛选）or 公共模式（简洁） |
| `/chat-history` | AI对话记录 | 全部AI对话历史浏览、分组展示、搜索 |
| `/diagnose-detail/:index` | 诊断报告 | 完整的诊断报告详情、达标/缺失/建议展示 |

### 3.2 核心业务流程

**链路A：有简历用户**
```
首页 → 简历智能处理（上传/制作） → 岗位智能匹配 → JD简历诊断优化 → 个人中心
```

**链路B：无简历用户**
```
首页 → AI职业规划咨询 → 职业测评 → 在线制作简历 → 岗位匹配
```

**链路C：求职沉淀**
```
任意节点 → 心路日志 → 快速记录 → 标签管理 → 日历复盘 → AI洞察分析
```

**链路D：社区互助**
```
笔记社区 → 分类浏览 → 点赞收藏评论 → 查看详情 → 自主发布分享
```

### 3.3 特色功能

#### 笔记双模式
- **个人模式**（从个人中心进入）：带侧边栏，支持搜索、分类筛选、日期范围选择、排序切换、日历视图、笔记列表完整展示
- **公共模式**（从社区进入）：简洁居中布局，无侧边栏，专注阅读体验

#### 日历双标注
日历视图中：
- 🔵 蓝点 = 该天有笔记
- 🟢 绿点 = 该天有日志
- 🟣 紫点 = 两者都有

#### 数据同步
- 注册/登录后自动同步 Supabase 云端数据
- 每次数据变更异步推送到云端
- 跨设备登录自动合并

---

## 四、数据模型设计

### 4.1 数据库表结构

#### user_data 表（用户数据存储）

```sql
CREATE TABLE user_data (
  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  user_id TEXT NOT NULL UNIQUE,    -- 用户唯一标识
  username TEXT,                    -- 用户名
  data JSONB DEFAULT '{}'::jsonb,  -- JSON 存储所有业务数据
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

`data` 字段存储内容：

```json
{
  "resumes": [ ... ],           // 简历列表
  "journals": [ ... ],          // 心路日志
  "my_notes": [ ... ],          // 个人笔记
  "favorites": [ ... ],         // 收藏岗位
  "ai_chat": [ ... ],           // AI对话记录
  "profile": { ... },           // 个人资料
  "diagnose_history": [ ... ],  // 诊断历史
  "career_tags": { ... },       // 职业标签
  "liked_notes": [ ... ],       // 点赞的笔记ID
  "liked_articles": [ ... ]     // 点赞的文章ID
}
```

#### community_notes 表（社区笔记）

```sql
CREATE TABLE community_notes (
  id BIGINT NOT NULL PRIMARY KEY,
  author TEXT DEFAULT '',
  title TEXT DEFAULT '',
  content TEXT DEFAULT '',
  category TEXT DEFAULT '其他',     -- 面试经验/简历技巧/职场心得/学习资源
  cover TEXT DEFAULT '',
  date TEXT DEFAULT '',
  likes INTEGER DEFAULT 0,
  comments JSONB DEFAULT '[]'::jsonb  -- [{author, content, time}]
);
```

### 4.2 数据分层隔离

```
┌──────────────────────────────────────────────────────────────┐
│                     平台公共数据                               │
│  岗位池(模拟) / 干货文章 / 测评链接 / 社区笔记(公开)           │
│  所有用户共享浏览                                              │
├──────────────────────────────────────────────────────────────┤
│                     用户私有数据                               │
│  简历 / 日志 / AI对话 / 收藏 / 诊断报告 / 个人资料             │
│  仅本人可见，绑定 user_id                                      │
├──────────────────────────────────────────────────────────────┤
│                     社区公开数据                               │
│  社区笔记 / 评论 / 点赞记录                                    │
│  全员可浏览，作者可编辑删除                                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、部署方案

### 5.1 Vercel 部署（推荐）

1. 代码推送到 GitHub 仓库
2. 登录 [Vercel](https://vercel.com) → Import Project
3. 选择仓库，Framework 自动识别为 Vite
4. 配置环境变量（Supabase URL/Key、AI API Key）
5. Deploy → 自动生成 `https://xxx.vercel.app`

**优势：** 自动 HTTPS、Git 推送自动部署、全球 CDN

### 5.2 Cloudflare Pages 部署（国内访问快）

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com)
2. Pages → Create a project → Connect to Git
3. 构建命令：`npm run build`，输出目录：`dist`
4. 配置环境变量
5. Deploy → 自动生成 `https://xxx.pages.dev`

**优势：** 国内节点访问快、免费 SSL、DDoS 防护

### 5.3 环境变量清单

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `VITE_SUPABASE_URL` | Supabase 项目地址 | `https://xxx.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | Supabase 匿名公钥 | `eyJhbGciOiJIUzI1NiIs...` |
| `VITE_AI_API_KEY` | AI 服务 API Key | `sk-xxx` |
| `VITE_AI_API_URL` | AI 服务地址 | `https://api.siliconflow.cn/v1/chat/completions` |
| `VITE_AI_MODEL` | AI 模型 | `deepseek-ai/DeepSeek-V4-Flash` |

### 5.4 数据库初始化

Supabase 项目创建后，在 SQL Editor 中执行 `supabase_migration.sql` 建表。

---

## 六、安全性设计

### 6.1 数据安全
- 密码 SHA-256 哈希加密存储
- Supabase Row Level Security 行级权限控制
- API 密钥通过环境变量注入，前端不硬编码
- 用户数据按 user_id 隔离

### 6.2 输入校验
- 用户名格式校验（3-20位，中英文数字下划线）
- 注册时分布式用户名唯一性校验（本地 + Supabase）
- 内容长度限制
- 特殊字符过滤

### 6.3 合规约束
- 第三方测评仅外链跳转，无数据交互
- 岗位数据为模拟参考，不提供投递服务
- 隐私政策与服务条款弹窗提示
- 不收集手机号、邮箱等敏感个人信息

---

## 七、项目文件结构

```
offer-catcher/
├── public/
│   └── _redirects              # Cloudflare SPA 路由配置
├── src/
│   ├── components/              # 通用组件
│   │   ├── NavHeader.vue       # 顶部导航
│   │   ├── Footer.vue          # 底部页脚（含使用指南/FAQ/隐私政策/服务条款）
│   │   └── AIFloatButton.vue   # 悬浮AI按钮
│   ├── views/                  # 11个页面视图
│   │   ├── Home.vue            # 首页
│   │   ├── Resume.vue          # 简历智能处理
│   │   ├── JobMatch.vue        # 岗位智能匹配
│   │   ├── ResumeCheck.vue     # JD简历诊断优化
│   │   ├── AIConsult.vue       # AI职业规划咨询
│   │   ├── Journal.vue         # 求职心路日志
│   │   ├── Community.vue       # 求职笔记社区
│   │   ├── Article.vue         # 求职干货
│   │   ├── User.vue            # 个人中心
│   │   ├── NoteDetail.vue      # 笔记详情页（双模式）
│   │   ├── ChatHistory.vue     # AI对话记录详情
│   │   └── DiagnoseDetail.vue  # 诊断报告详情
│   ├── stores/
│   │   └── user.js             # 用户状态 + 数据读写
│   ├── utils/
│   │   ├── supabase.js         # Supabase 客户端
│   │   ├── supabaseStorage.js  # Supabase 云端存储封装
│   │   ├── aiService.js        # AI 服务封装
│   │   ├── crypto.js           # 密码哈希
│   │   ├── markdown.js         # Markdown 渲染
│   │   └── mockData.js         # 模拟数据
│   ├── router/
│   │   └── index.js            # 12条路由配置
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── .env.example                # 环境变量模板
├── .gitignore
├── index.html
├── package.json
├── vite.config.js
├── vercel.json                 # Vercel SPA 路由配置
├── wrangler.toml               # Cloudflare Pages 配置
├── supabase_migration.sql      # 数据库建表脚本
└── 方案说明文档.md               # 本文档
```

---

## 八、版本记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2026-06 | 核心功能上线：AI咨询/简历/匹配/诊断/日志/社区/个人中心 |
| v1.1 | 2026-06 | 新增笔记详情页+侧边栏筛选、AI对话记录、诊断报告详情 |
| v1.2 | 2026-06 | 社区笔记整理、日历标注优化、个人/公共双模式布局 |
| v1.3 | 2026-06 | 接入 Supabase 云端数据库，数据持久化跨设备同步 |

---

## 九、总结

本项目基于 Vue 3 + Element Plus 构建，采用极简免密登录体系，集成 AI 大模型能力，通过 **localStorage + Supabase 双写策略** 实现数据本地秒级读写与云端持久化的最佳平衡。部署于 Vercel / Cloudflare Pages 免费平台，零成本公网可达。

**核心亮点：**
1. 一站式求职平台，覆盖 AI 咨询 → 简历 → 匹配 → 诊断 → 沉淀全链路
2. AI 驱动的智能匹配与优化，真实大模型 API 调用
3. 双模式笔记（个人/公共）+ 日历双标注（笔记/日志）
4. localStorage + Supabase 双写策略，兼顾速度与持久化
5. 完全免费的云端部署方案，可同时部署 Vercel + Cloudflare Pages
