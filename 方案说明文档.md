# AI求职智能匹配平台 - 方案说明文档

## 一、项目概述

### 1.1 产品定位
AI职配助手是面向在校大学生、应届生、实习求职群体的一站式AI求职成长平台，覆盖职业迷茫疏导、无方向规划、简历智能优化、岗位精准匹配、个人求职沉淀、同龄人社区互助全场景。

### 1.2 核心价值
- 解决求职迷茫痛点：AI智能引导梳理职业方向
- 提升求职效率：多维度精准匹配适配岗位
- 优化简历质量：AI诊断+针对性优化建议
- 沉淀求职成长：私密日志+公开社区双轨沉淀

## 二、技术架构

### 2.1 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | Vue | 3.4.x | 渐进式JavaScript框架 |
| 构建工具 | Vite | 5.1.x | 下一代前端构建工具 |
| UI组件 | Element Plus | 2.14.x | Vue3组件库 |
| 路由管理 | Vue Router | 4.3.x | Vue路由管理 |
| 数据库 | Supabase | 免费版 | 云端PostgreSQL数据库 |
| AI服务 | 硅基流动API | - | DeepSeek-V4-Flash大模型 |
| 部署平台 | Cloudflare Pages | 免费版 | 前端静态托管 |

### 2.2 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层                              │
│  Vue3 + Element Plus + Vue Router                             │
│  9大页面：首页/简历/岗位匹配/JD诊断/AI咨询/日志/社区/干货/个人中心│
├─────────────────────────────────────────────────────────────────┤
│                        业务逻辑层                              │
│  用户状态管理 / AI服务封装 / 工具函数                          │
├─────────────────────────────────────────────────────────────────┤
│                        数据访问层                              │
│  Supabase SDK / 模拟数据 / LocalStorage缓存                   │
├─────────────────────────────────────────────────────────────────┤
│                        后端服务层                              │
│  Supabase Auth / PostgreSQL / AI API                          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 关键设计

#### 登录体系设计
- **极简免密登录**：仅用户名注册登录，无密码、无手机、无邮箱
- **后端唯一性校验**：注册时实时全局查重，杜绝数据串档
- **双用户模式**：游客模式浏览公开内容，登录解锁全部功能
- **数据隔离**：私有数据与社区数据完全隔离

#### AI服务中间层设计
```javascript
// aiService.js 预留切换接口
const aiService = {
  provider: 'siliconflow',  // 可切换为其他服务商
  model: 'deepseek-ai/DeepSeek-V4-Flash',
  
  async chat(messages) { /* 统一调用接口 */ },
  async analyzeResume(text) { /* 简历解析 */ },
  async diagnoseResume(resume, jd) { /* JD诊断 */ }
}
```

## 三、核心功能模块

### 3.1 页面路由总览

| 路径 | 页面名称 | 核心功能 |
|------|----------|----------|
| / | 首页 | 平台入口、快捷入口、热门岗位 |
| /resume | 简历智能处理 | 上传解析、在线制作、云端管理 |
| /job-match | 岗位智能匹配 | 三重维度匹配、分层展示、筛选收藏 |
| /resume-check | JD简历诊断优化 | AI体检报告、针对性优化建议 |
| /ai-consult | AI职业规划咨询 | 智能对话、标签提炼、测评外链 |
| /journal | 求职心路日志 | Flomo式私密记录、标签管理、时间线 |
| /community | 求职笔记社区 | 图文发布、互动体系、分类检索 |
| /article | 求职干货 | 攻略聚合、分类浏览 |
| /user | 个人中心 | 简历管理、收藏台账、数据设置 |

### 3.2 核心业务流程

**链路A：有简历用户**
```
首页 → 简历智能处理 → 岗位智能匹配 → JD简历诊断优化 → 个人中心
```

**链路B：无简历用户**
```
首页 → AI职业规划咨询 → 职业测评 → 一键生成简历 → 岗位匹配
```

**链路C：求职沉淀**
```
任意节点 → 求职心路日志 → 极速记录 → 时间线复盘
```

**链路D：社区互助**
```
社区浏览 → 点赞收藏评论 → 自主发布 → 互助成长
```

## 四、数据模型设计

### 4.1 核心数据表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| users | 用户信息 | username(PK), created_at |
| resumes | 简历数据 | user_id, content, version |
| journals | 心路日志 | user_id, content, tags, pinned |
| community_notes | 社区笔记 | author_id, title, content, likes |
| comments | 评论数据 | note_id, author_id, content |
| favorites | 收藏记录 | user_id, job_id |
| jobs | 岗位参考池 | name, company, location, salary |
| ai_conversations | AI对话记录 | user_id, messages |

### 4.2 数据分层隔离

```
┌─────────────────────────────────────────────────────────────┐
│                    平台公共数据                              │
│  jobs(岗位池) / articles(干货文章) / test_links(测评链接)     │
│  所有用户共享浏览                                            │
├─────────────────────────────────────────────────────────────┤
│                    用户私有数据                              │
│  resumes / journals / ai_conversations / favorites          │
│  绑定username，仅本人可见                                    │
├─────────────────────────────────────────────────────────────┤
│                    社区公开数据                              │
│  community_notes / comments / likes                         │
│  全员可浏览，仅作者可编辑删除                                │
└─────────────────────────────────────────────────────────────┘
```

## 五、部署方案

### 5.1 前端部署（Cloudflare Pages）

**配置步骤：**
1. 连接GitHub仓库
2. 构建命令：`npm run build`
3. 输出目录：`dist`
4. 环境变量配置：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `VITE_AI_API_KEY`
   - `VITE_AI_API_URL`

**优势：**
- 免费SSL证书，自动HTTPS
- 全球CDN加速
- 自动构建部署

### 5.2 后端部署（Supabase免费版）

**配置步骤：**
1. 创建Supabase项目
2. 创建数据表（SQL脚本）
3. 配置Row Level Security
4. 获取API密钥

**数据表创建脚本示例：**
```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR(20) UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE resumes (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  content JSONB,
  version INTEGER DEFAULT 1,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 5.3 心跳脚本配置

创建 `heartbeat.js` 定时请求防止休眠：
```javascript
// 每10分钟发送一次请求
setInterval(() => {
  fetch('https://your-project.supabase.co/rest/v1/users?limit=1')
    .then(res => res.json())
    .catch(err => console.log('Heartbeat error:', err))
}, 600000)
```

## 六、安全性设计

### 6.1 数据安全
- 用户数据加密存储（Supabase自动加密）
- 敏感词过滤（社区内容）
- 权限隔离（私有数据仅本人可见）
- API密钥环境变量存储，前端不暴露

### 6.2 输入校验
- 用户名格式校验（3-20位，中英文数字）
- 后端唯一性校验
- 内容长度限制
- 特殊字符过滤

### 6.3 合规约束
- 第三方测评仅外链跳转，无数据交互
- 岗位仅供参考，不提供投递服务
- 隐私政策与服务条款提示

## 七、开发约束清单

| 约束项 | 要求 | 状态 |
|--------|------|------|
| 招聘平台对接 | V1.0严禁对接 | ✅ |
| 日志与社区隔离 | 完全隔离，无互通 | ✅ |
| 测评数据采集 | 仅外链跳转，无采集 | ✅ |
| 登录规则 | 仅用户名，无密码手机 | ✅ |
| 部署成本 | 全程免费资源 | ✅ |

## 八、项目文件结构

```
src/
├── components/          # 通用组件
│   ├── NavHeader.vue    # 顶部导航
│   ├── Footer.vue       # 底部页脚
│   └── AIFloatButton.vue # 悬浮AI按钮
├── views/              # 页面视图
│   ├── Home.vue        # 首页
│   ├── Resume.vue      # 简历智能处理
│   ├── JobMatch.vue    # 岗位智能匹配
│   ├── ResumeCheck.vue # JD简历诊断
│   ├── AIConsult.vue   # AI职业咨询
│   ├── Journal.vue     # 心路日志
│   ├── Community.vue   # 求职社区
│   ├── Article.vue     # 求职干货
│   └── User.vue        # 个人中心
├── stores/             # 状态管理
│   └── user.js         # 用户状态
├── utils/              # 工具函数
│   ├── supabase.js     # Supabase配置
│   ├── aiService.js    # AI服务封装
│   └── mockData.js     # 模拟数据
├── router/             # 路由配置
│   └── index.js
├── App.vue             # 根组件
├── main.js             # 入口文件
└── style.css           # 全局样式
```

## 九、总结

本项目基于Vue3+Element Plus构建，采用极简免密登录体系，集成AI大模型能力，实现了完整的求职匹配平台功能。通过Cloudflare Pages和Supabase免费版实现零成本公网部署，符合赛事评审演示需求。

**核心亮点：**
1. 一站式求职平台，覆盖全场景需求
2. AI驱动的智能匹配与优化
3. 极简登录体验，零门槛使用
4. 完全免费的云端部署方案
5. 严格的数据隔离与安全设计